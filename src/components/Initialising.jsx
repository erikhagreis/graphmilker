import React from 'react';
import './initialising.css';

export default () => (
  <div className="initialising">
    <h2 className="initialising__title sectionTitle">Hold on, initialising...</h2>
  </div>
);
